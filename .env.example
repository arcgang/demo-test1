# Next.js environment
NODE_ENV=development

# Database (required if a persistent store is added; not used by the current in-memory implementation)
DATABASE_URL=postgresql://user:password@localhost:5432/sdlc_db
